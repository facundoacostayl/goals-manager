describe("goal", () => {
  describe("get a goal", () => {
    describe("given goal is not found", () => {
      it("should return true", () => {
        expect(true).toBe(true);
      });
    });
  });
});
